# Comma-separated list of origins allowed to call this API.
# In development, the Vite dev server usually runs on port 5173.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Directory (relative to backend/) containing the trained model files.
MODELS_DIR=models
